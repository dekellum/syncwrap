127.0.0.1       localhost
::1             ip6-localhost localhost6

# Hosts generated by SyncWrap::EtcHosts
<% etc_host_table.each do |ip, names| %>
<%= "%-15s %s" % [ ip, names.join( ' ' ) ] %>

<% end %>
